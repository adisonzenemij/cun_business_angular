import { TestBed } from '@angular/core/testing';
import { FtB52d40d1 } from './ft-b52d40d1';

describe('FtB52d40d1', () => {
  let service: FtB52d40d1;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtB52d40d1);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
