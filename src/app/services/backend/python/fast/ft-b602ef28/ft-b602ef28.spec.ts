import { TestBed } from '@angular/core/testing';
import { FtB602ef28 } from './ft-b602ef28';

describe('FtB602ef28', () => {
  let service: FtB602ef28;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtB602ef28);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
