import { TestBed } from '@angular/core/testing';
import { FtA6aedeb5 } from './ft-a6aedeb5';

describe('FtA6aedeb5', () => {
  let service: FtA6aedeb5;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtA6aedeb5);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
