import { TestBed } from '@angular/core/testing';
import { FtA1fecd50 } from './ft-a1fecd50';

describe('FtA1fecd50', () => {
  let service: FtA1fecd50;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtA1fecd50);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
