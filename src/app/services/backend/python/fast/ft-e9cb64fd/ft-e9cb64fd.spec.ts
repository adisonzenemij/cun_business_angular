import { TestBed } from '@angular/core/testing';
import { FtE9cb64fd } from './ft-e9cb64fd';

describe('FtE9cb64fd', () => {
  let service: FtE9cb64fd;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtE9cb64fd);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
