import { TestBed } from '@angular/core/testing';
import { FtB64883b6 } from './ft-b64883b6';

describe('FtB64883b6', () => {
  let service: FtB64883b6;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FtB64883b6);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
